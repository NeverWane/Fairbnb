import { EmitRefDirective } from './emit-ref.directive';

describe('EmitRefDirective', () => {
  it('should create an instance', () => {
    const directive = new EmitRefDirective();
    expect(directive).toBeTruthy();
  });
});
